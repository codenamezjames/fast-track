# Fast Track Production Environment
# Copy to .env and fill in secure values

# MongoDB (use alphanumeric only to avoid URL encoding issues)
# Generate with: openssl rand -hex 16
MONGO_PASSWORD=change-this-to-secure-password

# JWT Secrets (generate with: openssl rand -hex 32)
JWT_SECRET=generate-a-secure-secret
JWT_REFRESH_SECRET=generate-another-secure-secret

# CORS Origin (comma-separated for multiple origins)
# Include both local and public domains if using Cloudflare tunnel
CORS_ORIGIN=http://theflyingdutchman.local:8088,https://ft.theflyingdutchman.online

# API URL for frontend build
# Use public domain if accessing via Cloudflare tunnel
VITE_API_URL=https://api-ft.theflyingdutchman.online/api
